<Application x:Class="ZenIT.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <Color x:Key="ZenTealColor">#00A7A5</Color>
        <Color x:Key="ZenTealDarkColor">#006D73</Color>
        <Color x:Key="ZenNavyColor">#172B3A</Color>
        <Color x:Key="ZenMutedColor">#6B7A8A</Color>
        <Color x:Key="ZenSuccessColor">#20B486</Color>
        <Color x:Key="ZenWarningColor">#F5A623</Color>
        <Color x:Key="ZenErrorColor">#E5484D</Color>

        <SolidColorBrush x:Key="ZenTealBrush" Color="{StaticResource ZenTealColor}" />
        <SolidColorBrush x:Key="ZenTealDarkBrush" Color="{StaticResource ZenTealDarkColor}" />
        <SolidColorBrush x:Key="ZenNavyBrush" Color="{StaticResource ZenNavyColor}" />
        <SolidColorBrush x:Key="ZenMutedBrush" Color="{StaticResource ZenMutedColor}" />
        <SolidColorBrush x:Key="ZenCardBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="ZenStrokeBrush" Color="#DCEAF4" />
        <SolidColorBrush x:Key="ZenSuccessBrush" Color="{StaticResource ZenSuccessColor}" />
        <SolidColorBrush x:Key="ZenWarningBrush" Color="{StaticResource ZenWarningColor}" />
        <SolidColorBrush x:Key="ZenErrorBrush" Color="{StaticResource ZenErrorColor}" />

        <DropShadowEffect x:Key="CardShadow"
                          BlurRadius="20"
                          Direction="270"
                          Opacity="0.10"
                          ShadowDepth="6"
                          Color="#55738A" />

        <DropShadowEffect x:Key="CardHoverShadow"
                          BlurRadius="26"
                          Direction="270"
                          Opacity="0.16"
                          ShadowDepth="9"
                          Color="#55738A" />

        <DropShadowEffect x:Key="ButtonShadow"
                          BlurRadius="14"
                          Direction="270"
                          Opacity="0.12"
                          ShadowDepth="4"
                          Color="#55738A" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource ZenNavyBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="MinHeight" Value="40" />
            <Setter Property="MinWidth" Value="130" />
            <Setter Property="Height" Value="42" />
            <Setter Property="Padding" Value="20,0" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="{StaticResource ZenTealBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource ZenTealBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="KeyboardNavigation.IsTabStop" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="20"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1" />
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource ZenTealDarkBrush}" />
                                <Setter Property="BorderBrush" Value="{StaticResource ZenTealDarkBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.88" />
                                <Setter TargetName="Root" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.985" ScaleY="0.985" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource ZenNavyBrush}" />
                                <Setter TargetName="Root" Property="BorderThickness" Value="2" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#A9BDC7" />
                                <Setter Property="BorderBrush" Value="#A9BDC7" />
                                <Setter Property="Foreground" Value="#F3F7FA" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="10" />
            <Setter Property="MinWidth" Value="10" />
            <Setter Property="Background" Value="Transparent" />
        </Style>
    </Application.Resources>
</Application>
